= form_for @food_type do |f|
  -if @food_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@food_type.errors.count, "error")} prohibited this food_type from being saved:"
      %ul
        - @food_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
